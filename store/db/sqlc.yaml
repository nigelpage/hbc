version: "2"
cloud:
  project: "01KE0Z42CCYB03DJ10YF8KB4MD"
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: "postgresql"
    name: "hbc"
    database:
      uri: "postgres://${PG_USERNAME}:${PG_PASSWORD}@localhost:5432/hbc?sslmode=disable"
    gen:
      go:
        package: "dbstore"
        out: "./"
        sql_package: "pgx/v5"
