version: "2"
cloud:
  project: "01KE0Z42CCYB03DJ10YF8KB4MD"
servers:
  - engine: postgresql
    uri: "postgres://${PGUSER}:${PGPASSWORD}@localhost:5432/${DBNAME}"
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: "postgresql"
    name: "hbc"
    database:
      managed: true
    gen:
      go:
        package: "dbstore"
        out: "./"
        sql_package: "pgx/v5"
